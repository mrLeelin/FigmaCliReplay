# Figma MCP Relay - 清理任务超时修复方案

## 问题诊断

### 根本原因
UI 端在 `executeJob()` 完成后立即发送 `command.response`，但 `executeJob()` 只是将消息转发给插件端，插件端的清理事务（`handleFigmaHierarchyCleanupTransaction`）是异步执行的，可能需要几秒钟。

服务器收到 `command.response` 后认为任务完成，但实际清理工作还在进行中，5分钟后租约过期。

### 当前流程
```
WebSocket 任务到达
  → UI: executeJob() [仅转发消息]
  → UI: 发送 command.response ❌ (过早)
  → 插件: handleFigmaHierarchyCleanupTransaction() [异步执行]
  → 插件: 发送 FIGMA_HIERARCHY_CLEANUP_TRANSACTION_RESULT
  → UI: 通用 _RESULT 处理器
  → UI: postResult() 通过 HTTP 回传
```

### 期望流程
```
WebSocket 任务到达
  → UI: executeJob() [仅转发消息]
  → 插件: handleFigmaHierarchyCleanupTransaction() [异步执行]
  → 插件: 发送 FIGMA_HIERARCHY_CLEANUP_TRANSACTION_RESULT
  → UI: 通用 _RESULT 处理器
  → UI: postResult() 通过 HTTP 回传
  → UI: 发送 command.response ✅ (正确时机)
```

## 修复方案

### 方案 A：延迟 command.response 直到收到 _RESULT（推荐）

**修改位置：** `ui.html` 第4145-4162行和第5805-5825行

**核心思路：**
1. `executeJob()` 完成后**不再发送** `command.response`
2. 在通用 `_RESULT` 处理器中，回传结果后**才发送** `command.response`

**优点：**
- 适用于所有需要等待插件响应的任务
- 逻辑清晰，修改最小

**实现：**

修改 `processRelaySocketQueue()` (4145-4162行)：
```javascript
executeJob(payload)
  .then(function () {
    // 不再在这里发送 command.response
    // 等待插件端的 _RESULT 消息
  })
  .catch(function (error) {
    // 错误情况下立即发送 command.response
    clearExecutionWatchdog();
    isExecuting = false;
    var result = { status: "error", errors: [error.message || String(error)], warnings: [] };
    lastResultPayload = { requestId: payload.requestId, result: result };
    postResult(payload.requestId, result).catch(function () {});
    if (relaySocket && relaySocket.readyState === WebSocket.OPEN) {
      relaySocket.send(JSON.stringify({ 
        type: "command.response", 
        id: payload.requestId, 
        requestId: payload.requestId, 
        error: error.message || String(error) 
      }));
    }
    scheduleNextPoll(500);
    setTimeout(processRelaySocketQueue, 0);
  });
```

修改通用 `_RESULT` 处理器 (5805-5825行)：
```javascript
if (message.type && message.type.endsWith("_RESULT") && message.type !== "EXPORT_IMAGES_TO_UNITY_RESULT") {
  isExecuting = false;
  clearExecutionWatchdog();
  executingRequestId = "";
  executingJobType = "";
  var result = message.result || {};
  lastResultPayload = { requestId: message.requestId, result: result };
  (async function () {
    try {
      await postResult(message.requestId, result);
      var summary = typeof result.summary === "object" ? JSON.stringify(result.summary) : (result.createdCount + " 节点");
      appendLog("任务完成 (" + message.type + ")：" + (result.status || "unknown") + "，" + summary);
      setAiStatus("任务完成：" + (result.status || "unknown"));
      
      // 在结果回传成功后发送 command.response
      if (relaySocket && relaySocket.readyState === WebSocket.OPEN) {
        relaySocket.send(JSON.stringify({ 
          type: "command.response", 
          id: message.requestId, 
          requestId: message.requestId, 
          accepted: true 
        }));
      }
    } catch (e) {
      appendLog("任务完成但结果回传失败：" + (e.message || e));
    }
    scheduleNextPoll(500);
    processRelaySocketQueue();
  })();
  return;
}
```

### 方案 B：增加超时时间（临时方案，不推荐）

修改 `src/runtimeRelay.ts` 第24行：
```typescript
const JOB_LEASE_MS = 10 * 60 * 1000;  // 从 5分钟改为 10分钟
```

**缺点：**
- 只是延长超时，没有解决根本问题
- 如果清理复杂节点需要更长时间，还是会超时

## 测试验证

### 测试步骤
1. 选择一个包含多层嵌套的 Figma 节点
2. 通过 MCP 调用 AI 清理功能
3. 观察日志：
   - 服务器日志应该显示任务从 `in_flight` → `completed`
   - 不应该再出现 "Relay job lease expired"
4. 验证清理结果正确应用到 Figma

### 预期结果
- ✅ 清理任务成功完成
- ✅ 服务器日志无超时警告
- ✅ 清理操作正确应用

## 风险评估

### 低风险
- 修改仅影响 WebSocket 任务的响应时机
- 错误处理路径保持不变
- 向后兼容轮询模式（不使用 WebSocket 的情况）

### 需要注意
- 如果插件端卡死不返回 `_RESULT`，UI 会一直等待（但有 8秒 watchdog 超时保护）
- 确保所有任务类型都能正确发送 `_RESULT`
