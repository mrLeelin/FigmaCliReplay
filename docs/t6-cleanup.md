# T6 清理执行边界

删除 MCP SDK、服务和用户配置写入链；所有旧 HTTP 业务入口拒绝执行。日志、状态、打开目录迁到共享 WS 控制，受控资源下载保留。删除旧 Python 常驻服务和客户端，重命名已采用 CLI 的技能入口并同步调用方。启动、诊断、发布脚本改为独立 Relay 与项目 CLI；不操作当前常驻服务、不改用户全局配置。

验证包括隔离端口的旧路由拒绝、共享控制日志/状态查询、资源权限、CLI 握手、Python 算法、启动脚本语法、完整构建及全量回归。历史设计文档可保留 MCP 术语；持久化 Figma metadata 命名空间暂保留数据兼容，不属于协议入口。

## 2026-09-18 验证结果

- `npm run build` 通过，开发构建号 281；发布版本保持 0.1.48。
- Python `unittest discover`：29/29 通过。
- Node 全量（包含实际 C# 传输 fixture）：440 项，434 通过，6 失败，无跳过。日志：`.tmp/t6-final.log`。
- 6 项既有失败：PSD 组件库降级 1 项、staging/布局诊断 3 项、文本布局及回滚 2 项。未修改这些业务规则，不能宣称全量绿色。
- 最后一次新增 CLI 状态/日志的真实 WS 成功与拒绝测试已纳入全量；旧 HTTP 业务拒绝、资源来源/登记校验、日志时间参数验证通过。
- 干净目录 `npm ci --ignore-scripts` 成功，隔离依赖下 CLI `--help` 可运行。构建后 `dist/mcpServer.js`、`dist/mcpConfig.js` 不存在；锁文件无 MCP SDK。
- PowerShell 启动、诊断、打包脚本语法通过；无效端口启动拒绝记录同一 operationId 的开始/失败，不启动服务。有效启动没有对常驻端口执行。
- 活跃源码、脚本和技能文档无旧 `/mcp`、`start_mcp`、旧 Python 客户端/服务模块引用；AI CLI 外部工具禁用参数和通用日志解析可保留 MCP 字样，历史 metadata 字段不改写。

交付边界：完成 T6 本地迁移与验证，未执行版本提升、发布归档、Git 提交、真实 Relay 重启或 Unity 项目 Bridge 安装。T7 仍需真实 Figma/Unity 环境中的资源写入、断线和视觉验收；6 项 PSD 基线失败也仍需单独解决。
